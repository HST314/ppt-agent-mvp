# ADR-0004：HTML 构建与预览安全

状态：已采纳  
日期：2026-08-11

## 决策

生成 HTML 视为不受信任产物。预览必须运行在独立 sandbox iframe/等价隔离上下文；默认禁止脚本、表单、顶层导航、弹窗和任意网络请求。资源只允许通过当前任务 manifest 中的受控引用解析，并设置 CSP。预览服务不得直接暴露任意文件路径。

## 理由与后果

HTML 是产品核心产物，也是代码执行面。具体渲染框架由 HTML Skill/P4 决定，但不得弱化隔离底线。P4 必须加入脚本注入、远程资源、路径穿越和跨任务读取测试。
