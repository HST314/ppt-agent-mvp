# ADR-0001：运行方式

状态：已采纳  
日期：2026-08-11

## 决策

MVP 采用单体模块化后端和一个桌面 Web 工作区。领域契约、应用处理器、端口协议、基础设施适配器、API/Presenter 单向依赖。长动作由应用层作业抽象表示，P0/P1 不引入分布式队列；边界保留未来替换能力。

## 理由与后果

满足单机 MVP、恢复和测试要求，避免过早分布式化。进程崩溃恢复依赖持久化 checkpoint，不依赖内存会话。具体 Python Web 框架和前端构建工具留待 P1 API 草案按最小依赖选定。
