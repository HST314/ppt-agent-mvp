# ADR-0002：持久化与任务工作区

状态：已采纳  
日期：2026-08-11

## 决策

MVP 使用每任务隔离的文件工作区。输入快照、资源 manifest、领域产物、checkpoint、事件和交付包分区保存；写入采用临时文件加原子替换，任务内写操作持有进程锁。任何外部路径先规范化并验证仍位于授权根目录。

## 理由与后果

文件结构便于独立运行、检查与迁移，符合 MVP 范围。它不是多主机并发方案；未来可由 Store 协议替换为数据库/对象存储。P1 必须验证半写入恢复、跨任务隔离和目录穿越。
