# ADR-0003：模型与能力网关

状态：已采纳  
日期：2026-08-11

## 决策

生成模型、独立检查模型、Skill 加载器和 HTML 构建器均通过窄 Gateway 协议调用。每次调用保存配置与 Skill 版本快照、输入/Prompt hash、结果状态和诊断 ID。提供 deterministic fake；检查 Gateway 不继承生成上下文。结果未知的外部调用不得盲目重试。

## 理由与后果

隔离供应商差异、保证可测和可审计，并落实检查独立性。凭证只从环境注入，事件和用户错误不含敏感值、原始供应商异常或内部推理。
