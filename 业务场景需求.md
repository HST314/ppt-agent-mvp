# PPT Agent 业务场景需求

> 生效日期：2026-08-19  
> 本文是当前样品确认、全稿、自检修改、终稿与离线交付流程的业务基线；与旧文档冲突时以本文为准。

## 1. 目标

让用户在确认样品后连续完成全稿生成、可选自检与修改、终稿冻结和离线交付，减少重复点击，同时保留版本可追溯、任务可恢复和明确的人工终稿决策。

## 2. 主流程

1. 用户确认样品后，系统立即创建持久化的全稿生成 Job，并自动进入“全稿”页。
2. 全稿页只负责生成状态、整稿浏览和下一步决策，不提供修改表单。
3. 全稿生成成功后，用户可以：
   - 直接点击“确定终稿”，跳过检查进入交付；
   - 进入“自检与修改”，执行检查、Prompt 修改、问题处置、版本对比或回退后再确定终稿。
4. “确定终稿”必须由用户触发，绑定当时精确的 `deck_hash`，并记录检查状态。未检查、报告过期或仍有遗留问题均不阻断终稿确认，但必须如实记录。
5. 交付页只对已冻结终稿开放。用户点击“将离线包写入工程文件夹”后创建持久化 Job；成功写入并校验后任务才完成。
6. 已交付版本不可变。从交付历史派生修改时创建新候选，并进入“自检与修改”，原交付保持可追溯。

```text
确认样品 → 自动生成全稿 → 全稿浏览 ┬→ 确定终稿 → 交付写包
                                   └→ 自检与修改 → 确定终稿 → 交付写包
```

## 3. 自检与修改

- 独立检查是可选质量工具，不是交付门禁；仍保持与生成上下文隔离。
- 支持全检、增量检查、manual/auto 模式和有界自动修复。
- “Agent 修复”作为持久化 `inspection.fix` Job 执行；一次只修复选中的问题及其影响页面，不得改动无关页。
- 普通人工处置可选择手工已处理、接受/豁免或暂不处理，并保存依据。
- 所有 Prompt 修改、修复、回退和检查报告继续追加版本，不覆盖历史。

## 4. 图片与离线交付

候选 HTML 的展示性图片允许以下来源：

- `http://` 或 `https://` 图片 URL；
- PNG、JPEG、GIF、WebP 的 Base64 Data URL；
- 当前任务冻结资源清单中的安全相对路径；
- `background` / `background-image` 中的上述图片引用。

以下规则必须同时满足：

- 图片引用仅用于惰性展示，不开放脚本、iframe、事件属性、CSS `@import`、任意协议或凭据 URL。
- 预览使用禁止脚本的 CSP，并通过当前任务、版本 hash、资源清单和文件 hash 校验相对资源。
- 交付时下载远程图片并改写为包内相对资源；相对资源复制到包内；Base64 可保留内嵌。
- 远程下载限制协议、重定向、DNS/IP 范围、媒体类型、文件签名、单文件/总大小和数量，拒绝 SSRF 与资源炸弹。
- 任一资源本地化、原子发布或离线校验失败时，不得产生“已完成”的伪交付。
- 离线包不得依赖外部脚本、字体、图片或 CDN。

## 5. 可靠性与可访问性

- `deck.generate`、`inspection.run`、`inspection.fix`、`delivery.publish` 均为可恢复的持久化 Job；刷新后以服务端状态为准。
- 终稿事实与阶段推进在同一事务中提交；交付目录通过临时目录原子发布并校验 manifest/hash。
- 移动抽屉关闭后焦点回到菜单按钮，关闭时内容不可聚焦。
- `localStorage` 或 `sessionStorage` 因浏览器策略抛出 `SecurityError` 时，界面使用内存降级，不影响核心流程。
- 四档视口、键盘操作、减少动效、无横向溢出和真实 Chromium 完整旅程必须通过自动化门禁。

## 6. 验收结果

- 样品确认后不再出现第二个“生成完整演示稿”主动作。
- 全稿页不出现修改全稿表单；所有修改能力集中在“自检与修改”。
- 用户可在不运行检查的情况下冻结终稿，也可带报告过期或遗留问题状态冻结。
- 交付 Job 成功后，离线包通过 hash 与外部 URL 扫描；失败保持最后成功状态。
- Base64、相对路径、远程 URL 与 CSS 背景图片在真实浏览器预览中实际解码；远程/相对资源在交付包中可离线使用。
