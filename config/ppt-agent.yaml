# Safe local default: starts without credentials or network access.
# Copy config/ppt-agent.agent.example.yaml and set PPT_AGENT_CONFIG when using real models.
gateway:
  mode: fake

clarification:
  max_questions_per_round: 3
  max_rounds: 3
  style: comprehensive

skills:
  active: guizang-ppt-skill

capabilities:
  network_tools: false
  image_input: false
  image_generation: false
